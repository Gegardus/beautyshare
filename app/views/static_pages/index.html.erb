<section class="hero is-light is-fullheight">
  <% if params[:user_id].nil? %>
    <div class="m-8 hero-body">
      <div class="m-4 container has-text-centered">
        <%= render 'search_form' %>
      </div>
    </div>
  <% else %>
    <div class="container has-text-centered">
      <div class="m-4 container has-text-centered">
        <%= render 'search_form' %>
      </div>
      <% if params[:user_id].empty? %>
        <div class="container has-text-centered">
          Flickr API Searcher is displaying latest public photos uploaded to Flickr
        </div>
      <% else %>
        <div class="container has-text-centered">
          Flickr API Searcher is displaying public photos uploaded by <%= params[:user_id] %> 
        </div>
      <% end %>
    </div>
  <div id="user" class="mt-2 box container has-text-centered">
    <div id="user-info">
      <% @photos.each do |photo| %>
        <div class="col-md-auto">
          <img src="<%= Flickr.url_n(photo) %>"/>
        </div>
      <% end %>
    </div>
  </div>
  <% end %>
</section> 